#
# NOTE! Don't add files that are generated in specific
# subdirectories here. Add them in the ".gitignore" file
# in that subdirectory instead.
#
# NOTE! Please use 'git ls-files -i --exclude-standard'
# command after changing this file, to see if there are
# any tracked files which get ignored after the change.
#
# Normal rules
#
.*
*.o
*.o.*
*.a
*.s
*.ko
*.so
*.so.dbg
*.mod.c
*.i
*.lst
*.symtypes
*.order
modules.builtin
*.elf
*.bin
*.gz
*.bz2
*.lzma
*.lzo
*.patch
*.gcno
*.img

System.map
tags

#
# Top-level generic files
#
/tags
/TAGS
/Module.markers
/Module.symvers

#
# git files that we don't want to ignore even it they are dot-files
#
!.gitignore
#
# Generated include files
#
include/config
include/generated

# stgit generated dirs
patches-*
_install

# quilt's files
patches
series

# cscope files
cscope.*
ncscope.*

# gnu global files
GPATH
GRTAGS
GSYMS
GTAGS

*.orig
*~
\#*#

openssh-*/Makefile
openssh-*/buildpkg.sh
openssh-*/config.h
openssh-*/config.log
openssh-*/config.status
openssh-*/moduli.5.out
openssh-*/moduli.out
openssh-*/openbsd-compat/Makefile
openssh-*/openbsd-compat/regress/Makefile
openssh-*/openssh.xml
openssh-*/opensshd.init
openssh-*/scp
openssh-*/scp.1.out
openssh-*/sftp
openssh-*/sftp-server
openssh-*/sftp-server.8.out
openssh-*/sftp.1.out
openssh-*/ssh
openssh-*/ssh-add
openssh-*/ssh-add.1.out
openssh-*/ssh-agent
openssh-*/ssh-agent.1.out
openssh-*/ssh-keygen
openssh-*/ssh-keygen.1.out
openssh-*/ssh-keyscan
openssh-*/ssh-keyscan.1.out
openssh-*/ssh-keysign
openssh-*/ssh-keysign.8.out
openssh-*/ssh-pkcs11-helper
openssh-*/ssh-pkcs11-helper.8.out
openssh-*/ssh.1.out
openssh-*/ssh_config.5.out
openssh-*/ssh_config.out
openssh-*/sshd
openssh-*/sshd.8.out
openssh-*/sshd_config.5.out
openssh-*/sshd_config.out
openssh-*/survey.sh

